% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R, R/mve_sqsr.R
\name{mve_sqsr}
\alias{mve_sqsr}
\title{Compute Optimal Portfolio Square Sharpe Ratio}
\usage{
mve_sqsr(mu, sigma, selection, do_checks = FALSE)

mve_sqsr(mu, sigma, selection, do_checks = FALSE)
}
\arguments{
\item{mu}{Mean vector.}

\item{sigma}{Covariance matrix.}

\item{selection}{Asset selection vector (default = full set, 0-indexed).}

\item{do_checks}{Logical flag to perform input checks (default = FALSE).}
}
\value{
A scalar value corresponding to \eqn{\sqrt{\mu^T \Sigma^{-1}\mu}}.

A scalar value corresponding to \eqn{\sqrt{\mu^T \Sigma^{-1}\mu}}.
}
\description{
Given a mean vector \eqn{\mu}, a covariance matrix \eqn{\Sigma},
and an asset selection vector, this function computes the optimal Sharpe ratio defined as
\deqn{\sqrt{\mu^T \Sigma^{-1}\mu}}
over the selected assets.
If the provided asset selection vector has length less than N,
the function uses the subset of assets and computes the ratio using those assets.

Given a mean vector \eqn{\mu}, a covariance matrix \eqn{\Sigma},
and an asset selection vector, this function computes the optimal square
Sharpe ratio defined as
\deqn{\mu^T \Sigma^{-1}\mu}
over the selected assets.
If the provided asset selection vector has length less than N,
the function uses the subset of assets and computes the ratio using those assets.
}
\examples{
# Full selection example:
mve_sqsr(mu = c(0.1, 0.2, 0.15),
                       sigma = matrix(c(1, 0.2, 0.1,
                                          0.2, 1, 0.3,
                                          0.1, 0.3, 1), nrow = 3),
                       selection = c(0, 1, 2),
                       do_checks = TRUE)

# Subset selection example:
mve_sqsr(mu = c(0.1, 0.2, 0.15, 0.12),
                       sigma = diag(4),
                       selection = c(0, 2),
                       do_checks = TRUE)
}
