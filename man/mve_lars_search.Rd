% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mve_search.R
\name{mve_lars_search}
\alias{mve_lars_search}
\title{LARS heuristic for sparse MVE selection (covariance-based)}
\usage{
mve_lars_search(
  mu,
  sigma,
  n_obs,
  k,
  ridge_epsilon = 0,
  tol_nnl = 1e-10,
  normalize_weights = FALSE,
  normalization_type = 1L,
  use_refit = FALSE,
  compute_sr = FALSE,
  do_checks = FALSE
)
}
\arguments{
\item{mu}{Numeric mean vector.}

\item{sigma}{Numeric covariance matrix.}

\item{n_obs}{Number of observations underlying the moments.}

\item{k}{Target sparsity level.}

\item{ridge_epsilon}{Ridge factor for stabilization.}

\item{tol_nnl}{Threshold for treating coefficients as non-zero.}

\item{normalize_weights}{Logical; if TRUE, post-normalize weights via
normalize_weights_cpp(). Default normalization_type is L1.}

\item{normalization_type}{Integer; 1 = L1 (default), 0 = sum-to-one.}

\item{use_refit}{Logical; if TRUE, refit exact MVE weights on selected support.}

\item{do_checks}{Logical; if TRUE, validate inputs.}
}
\value{
List with selection (1-based indices), weights, sr, status.
}
\description{
Uses the \code{lars} package to compute a LASSO solution path on the synthetic
regression design implied by (mu, sigma, n_obs) under the mean--variance criterion.
}
\details{
Selection rule: (i) first solution with exactly k nonzeros (within tol_nnl),
else (ii) solution with the largest number of nonzeros not exceeding k (ties
broken in favor of the later point).
}
