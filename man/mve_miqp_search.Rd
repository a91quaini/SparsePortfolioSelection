% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mve_search.R
\name{mve_miqp_search}
\alias{mve_miqp_search}
\title{MIQP heuristic search for sparse selection (Gurobi backend)}
\usage{
mve_miqp_search(
  mu,
  sigma,
  k,
  exactly_k = FALSE,
  m = 1L,
  gamma = 1,
  fmin = -0.25,
  fmax = 0.25,
  expand_rounds = 20L,
  expand_factor = 3,
  expand_tol = 0.01,
  mipgap = 1e-04,
  time_limit = 200,
  threads = 1L,
  x_start = NULL,
  v_start = NULL,
  normalize_weights = FALSE,
  use_refit = FALSE,
  verbose = FALSE,
  ridge_epsilon = 0,
  do_checks = FALSE
)
}
\arguments{
\item{mu}{Numeric mean vector.}

\item{sigma}{Numeric covariance matrix.}

\item{k}{Target cardinality.}

\item{exactly_k}{Logical; if \code{TRUE}, enforce \eqn{\sum v_i = k} else banded.}

\item{m}{Lower bound on \eqn{\sum v_i} when \code{exactly_k=FALSE}.}

\item{gamma}{Risk-aversion weight on the quadratic term.}

\item{fmin, fmax}{Lower/upper bounds for each weight (scalar recycled to length N).}

\item{expand_rounds}{Number of progressive bound-expansion re-solves.}

\item{expand_factor}{Multiplicative factor for expanding active bounds.}

\item{expand_tol}{Tolerance for detecting bound hits.}

\item{mipgap}{Relative MIP gap.}

\item{time_limit}{Time limit in seconds.}

\item{threads}{Number of Gurobi threads (0 lets Gurobi decide).}

\item{x_start}{Optional warm-start weights.}

\item{v_start}{Optional warm-start binaries.}

\item{normalize_weights}{Logical; if \code{TRUE}, add budget constraint and
post-scale refit weights.}

\item{use_refit}{Logical; if \code{TRUE}, refit exact MVE on the selected support.}

\item{verbose}{Logical; emit Gurobi output.}

\item{ridge_epsilon}{Ridge for covariance stabilization.}

\item{do_checks}{Logical; validate inputs.}
}
\value{
List with \code{selection} (1-based), \code{weights}, \code{sr}, \code{status}.
}
\description{
Solves a quadratic MIP heuristic to select at most \code{k} assets (or exactly \code{k}
if \code{exactly_k=TRUE}). Objective is \eqn{0.5 * gamma * x' Sigma x - mu' x} with
linking constraints \eqn{fmin_i v_i \le x_i \le fmax_i v_i}, cardinality
constraints, optional budget \eqn{\sum x_i = 1} via \code{normalize_weights}, and
progressive bound expansion when solutions sit on caps. Uses the C++ Gurobi
backend; supports optional refit of exact MVE on the selected support.
}
