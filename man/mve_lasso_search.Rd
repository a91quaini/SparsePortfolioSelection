% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mve_search.R
\name{mve_lasso_search}
\alias{mve_lasso_search}
\title{LASSO relaxation heuristic for sparse MVE selection (moment-based)}
\usage{
mve_lasso_search(
  mu,
  sigma,
  n_obs,
  k,
  nlambda = 100L,
  lambda_min_ratio = 0.001,
  lambda = NULL,
  alpha = 1,
  n_folds = 5L,
  nadd = 80L,
  nnested = 2L,
  standardize = FALSE,
  epsilon = eps_ridge_cpp(),
  stabilize_sigma = TRUE,
  compute_weights = TRUE,
  normalize_weights = FALSE,
  use_refit = FALSE,
  do_checks = FALSE,
  R_cv = NULL
)
}
\arguments{
\item{mu}{Numeric mean vector.}

\item{sigma}{Numeric covariance matrix.}

\item{n_obs}{Sample size underlying the moments.}

\item{k}{Target cardinality.}

\item{nlambda}{Number of lambdas for glmnet path (ignored if \code{lambda} given).}

\item{lambda_min_ratio}{Smallest lambda as a fraction of lambda_max.}

\item{lambda}{Optional descending lambda grid.}

\item{alpha}{Elastic-net mixing parameter (1 = LASSO).}

\item{standardize}{Logical; whether glmnet standardizes the synthetic design.}

\item{epsilon}{Ridge for covariance stabilization.}

\item{stabilize_sigma}{Logical; symmetrize/ridge-stabilize sigma.}

\item{compute_weights}{Logical; return weights (else zeros).}

\item{normalize_weights}{Logical; post-scale weights via
\code{normalize_weights_cpp(mode="relative")}.}

\item{use_refit}{Logical; refit exact MVE on selected support.}

\item{do_checks}{Logical; validate inputs.}
}
\value{
List with \code{selection} (1-based), \code{weights}, \code{sr}, \code{status}, \code{lambda}, \code{alpha}.
}
\description{
Variant that takes precomputed moments (\code{mu}, \code{sigma}) and an assumed sample
size \code{n_obs}, builds the synthetic design directly, and runs the same glmnet
selection as the return-based version.
}
